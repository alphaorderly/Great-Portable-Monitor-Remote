cmake_minimum_required(VERSION 3.16)
if(DEFINED IDF_TARGET AND NOT IDF_TARGET STREQUAL "esp32s3")
    message(FATAL_ERROR "This project requires IDF_TARGET=esp32s3")
endif()
set(IDF_TARGET "esp32s3" CACHE STRING "Firmware target")
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(remote_ble_scan)
